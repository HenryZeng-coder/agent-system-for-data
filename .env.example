# 环境变量 — 复制为 .env 后填入真实值
# cp .env.example .env

DEEPSEEK_API_KEY=your_deepseek_api_key_here
DATABASE_URL=postgresql://localhost:5432/rating_system_national
PROXY_POOL_API=http://your-proxy-provider/api
HERMES_WORKDIR=/Users/henry/Desktop/E-InfoInsight-National

# ---- 采集范围 (可选, 优先级高于 config/config.yaml 的 region 段) ----
# national=全国 / province=按省 / city=按市
REGION_SCOPE=national
#REGION_PROVINCE=湖北
#REGION_CITY=武汉
